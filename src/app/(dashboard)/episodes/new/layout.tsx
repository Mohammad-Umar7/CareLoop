// The page is a client component, so its title is set here.
export const metadata = { title: 'Add patient' }

export default function AddPatientLayout({ children }: { children: React.ReactNode }) {
  return children
}
