/**
 * How much of a care plan fits in the WhatsApp message sent to the patient
 * (templates.ts). The rest is not lost: the assistant knows every warning sign
 * and instruction when the patient asks. Kept apart from the builder so the
 * review form can say what the patient will actually read.
 */
export const CARE_PLAN_MESSAGE_WARNINGS = 4
export const CARE_PLAN_MESSAGE_INSTRUCTIONS = 3
